import FixView from "@/components/basic/FixView";
import { Text, View } from "react-native";

const Register = () => {
  return (
    <FixView>
      <Text>Registrate querido padre de familia</Text>
    </FixView>
  );
};

export default Register;
